<!-- views/partials/header.ejs -->

<nav class="navbar navbar-default" role="navigation">
    <div class="container-fluid">

        <div class="navbar-header">
            <a class="navbar-brand" href="#">
                <img src="/public/images/Hookuplogo_header.png" />
            </a>
        </div>
        <img src="/public/images/Menu_44px.png" />
        <ul class="nav navbar-nav">
            <li><a href="/tn">Home</a></li>
            <% if (is_logged_in && (user.employee || user.admin)) { %>
            <li><a href="/tn/admin" class="btn btn-default btn-sm">Admin</a></li>
            <% } %>
            <% if (is_logged_in && (user.sexpert || user.admin)) { %>
            <li><a href="/tn/sexpert" class="btn btn-default btn-sm">Sexpert</a></li>
            <% } %>
            <% if (is_logged_in) { %>
            <li id="username"><%= user.username %></li>
            <li><a href="/logout" class="btn btn-default btn-sm">Logout</a></li>
            <% } %>
            <li><a href="/tn/ask">Ask a Sexpert</a></li>
            <li><a href="/tn/share">Share Your Story</a></li>
        </ul>
      
    </div>
</nav>
<% if (is_logged_in) { %>
    <input type="hidden" id="this_user_id" value= <%= user.id %> />
<% } %>